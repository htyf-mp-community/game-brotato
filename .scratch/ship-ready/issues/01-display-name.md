# 01: 给人看的名字改成「土豆兄弟」

**What to build:** 扫码列表、Godot 窗口、README 标题都显示「土豆兄弟」。`appid` 不动。不要手改 `dist/app.json`。

**Blocked by:** None (can start immediately)

**Status:** done

- [x] 根目录 `app.json` 的 `htyf.name` 为「土豆兄弟」
- [x] `project.godot` 的 `application/config/name` 为「土豆兄弟」
- [x] README 一级标题为「土豆兄弟」；文中「展示名」示例与之一致
- [x] `appid` 仍为现有值，没有改成新应用
